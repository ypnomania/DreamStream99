{
  "name": "dreamstream",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "A Windows 98-style YouTube watch-party UI with a GitHub Pages demo and a Serverless RoomClient architecture.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "node scripts/build-pages.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server/index.js && node --check server/room-state.js && node --check server/rooms.js && node --check public/runtime-config.js && node --check public/assets-config.js && node --check public/js/room-client.js && node --check public/js/desktop.js && node --check public/js/app.js && node --check public/js/youtube-adapter.js && node --check scripts/build-pages.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
